# 依赖
node_modules/

# 运行时配置（含真实 Token，不入库）
tunnel-server/config.json

# 系统文件
.DS_Store
*.swp

# 测试运行时状态（fakebin 模拟 settings 的持久化文件）
tests/fakebin/.settings.state*

# 测试运行时状态（fakebin 模拟输入后的界面文本）
tests/fakebin/.last_text
